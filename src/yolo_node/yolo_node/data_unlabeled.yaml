path: /home/elessar/russ_ws/ws7/src/yolo_node/yolo_node/datasets/with_unlabeled  # 数据集根目录
train: train/images  # 训练集图片相对路径
val: val/images      # 验证集图片相对路径
test: test/images    # 测试集图片相对路径

nc: 1

# class names
names: ['apical_four_chamber']

# 标签路径设置
train_labels: train/labels  # YOLO格式标签路径
val_labels: val/labels
test_labels: test/labels

